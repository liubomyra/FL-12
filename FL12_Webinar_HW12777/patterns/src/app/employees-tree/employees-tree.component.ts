import { Component, OnInit } from "@angular/core";

@Component({
  selector: "app-employees-tree",
  templateUrl: "./employees-tree.component.html",
  styleUrls: ["./employees-tree.component.scss"]
})
export class EmployeesTreeComponent implements OnInit {
  // rm_id: this.rm_id = rm_id
  // rmEmployees: this.rmEmployees = []
  // constructor() { }
  // ngOnInit(): void {
  // }
  // add(rmEmploye) {
  //   this.rmEmployees.push(rmEmploye)
  // }
  // getEmployeId(index) {
  //   return this.rmEmployees[index].rm_id;
  // }
  // display() {
  //   console.log(this.rm_id);
  //   for (let i = 0, length = this.rmEmployees.length; i < length; i++) {
  //       console.log("   ", this.getEmployeId(i));
  //   }
  // }
}
